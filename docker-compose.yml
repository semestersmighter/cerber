services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: html-frontend
    ports:
      - "8080:80"
    # depends_on:
    #   - backend
    restart: unless-stopped
